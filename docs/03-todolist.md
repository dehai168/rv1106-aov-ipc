# 开发任务清单（Agent Todolist）

> 状态标记：`[ ]` 待办 `[~]` 进行中 `[x]` 完成 `[!]` 阻塞。
> 领取任务时改为 `[~]`，完成后改 `[x]` 并附一句话结果（含 commit id）。
> 依赖关系已标注，禁止跳依赖开发。每次状态变更随代码一起 commit。

## M0 环境与骨架（里程碑：WSL 能编译出可在板上运行的 hello 程序）

- [x] T0.1 WSL 编译环境搭建 — 完成：SDK `824b817f8` + gcc8.3 工具链；`./build.sh media` 产出 `simple_vi_get_frame`；hello 板上打印成功；VI 例程识别 MIS5001（见 env.md）
  - 内容：WSL 内安装依赖，clone `LuckfoxTECH/luckfox-pico` SDK，完成 buildroot/工具链准备，编译通过官方任一 RKMPI 例程
  - 验收：交叉编译产物在板上可运行；`docs/env.md` 记录 SDK 路径、工具链版本、编译命令
- [x] T0.2 仓库骨架与构建系统 — 完成：`ipc_app` 板上输出 APP_START/exit 日志（commit 见 worklog）
  - 内容：按 `01-architecture.md` §5 建目录；CMake + `cmake/toolchain-rv1106.cmake`；`scripts/build.sh`、`scripts/deploy.ps1`（adb push + 运行）
  - 验收：`ipc_app` hello 版（打日志、装 signal handler、空事件总线）在板上跑起来，adb 能看到日志
- [x] T0.3 公共基础件 — 完成：`ipc_app --self-test` 板上 PASSED；配置落盘 `/userdata/ipc_config.json`，二次运行仍合并成功；日志 `/userdata/log/ipc_app.log`
  - 内容：日志模块（等级/文件输出到 /userdata/log）、配置中心（JSON 读写合并、原子写入，引入 cJSON 或等价轻量库）、事件总线（发布订阅）
  - 验收：单元级自测程序在板上通过；配置改写后重启保持

## M1 视频链路（里程碑：双码流稳定出流）

- [!] T1.1 RKAIQ + VI 初始化 — 链路已通（MIS5001 + AIQ + VI 1920x1080，NV12 落盘 `/mnt/sdcard/capture_0.yuv`）；**当前画面极暗（噪点级），待确认镜头遮挡/环境光照后再复抓验图**
  - 内容：media_service 初始化 ISP/3A 与 VI，确认 MIS5001 出图（先抓 NV12 帧存文件验证）
  - 验收：抓帧文件 PC 端查看图像正常（亮度/色彩正确）
- [ ] T1.2 双码流 VENC（依赖 T1.1）
  - 内容：主码流 H.265（2560x1440@15fps 起步）+ 子码流 H.264（704x576@15fps）；码流参数从配置中心读取
  - 验收：两路裸流各录 30s 存文件，PC 端可播放；板上连续跑 30 分钟无内存增长、无丢链路
- [ ] T1.3 编码参数动态调整（依赖 T1.2）
  - 内容：分辨率/码率/帧率/GOP 改配置后重建通道生效
  - 验收：改配置 → 调用 media API → 新参数生效且系统稳定

## M2 录像与存储（里程碑：TF 卡上产出可播放的分段 MP4，循环覆盖生效）

- [ ] T2.1 TF 卡管理（依赖 T0.3）
  - 内容：检测/挂载 TF 卡（确认内核 exFAT 支持，不支持则向用户汇报决策）、容量查询、目录结构创建、启动时坏尾文件清理
  - 验收：插卡自动挂载到 /mnt/sdcard；拔卡插卡状态正确
- [ ] T2.2 MP4 分段录像（依赖 T1.2, T2.1）
  - 内容：集成 minimp4，主码流 H.265 封装 MP4；按配置时长分段（默认 60s），每段 IDR 开头；文件名含时间戳
  - 验收：TF 卡取出插 PC，Windows 资源管理器直接双击播放正常，时长/时间戳正确
- [ ] T2.3 循环覆盖（依赖 T2.2)
  - 内容：低水位（默认剩余 10%）时删最旧录像；删除不阻塞写入
  - 验收：用小水位/大码率模拟写满，观察最旧文件被删且录像不中断
- [ ] T2.4 录像索引与查询（依赖 T2.2）
  - 内容：按日期/时间范围查询录像文件列表的内部 API（供 Web 回放用）
  - 验收：查询结果与卡上文件一致

## M3 移动侦测（里程碑：有人走动自动录像，静止后自动停止）

- [ ] T3.1 侦测帧通道（依赖 T1.1）
  - 内容：VI 第三通道低分辨率 NV12（约 640x360）供侦测
  - 验收：侦测线程稳定拿帧，帧率符合预期
- [ ] T3.2 移动侦测算法（依赖 T3.1）
  - 内容：首选 Rockit IVS 移动侦测；不可用则自研帧差+膨胀+连通域；输出运动事件（含区域）；灵敏度/区域/布防时间参数化
  - 验收：真机场景：人走动 2 秒内触发，静止场景 10 分钟零误报（普通室内光照）
- [ ] T3.3 侦测→录像状态机（依赖 T3.2, T2.2）
  - 内容：预录 GOP 环形缓冲（3–5s）；触发进 RECORDING（先落预录再实时）、静默 30s 回 IDLE；事件写告警记录+抓图
  - 验收：录像文件包含触发前画面；连续多次触发/停止无异常；告警记录与抓图正确
- [ ] T3.4 NPU 人形确认（可选增强，依赖 T3.2）
  - 内容：RKNN 集成轻量人形检测模型，对运动事件二次确认降误报；模型文件与转换流程记录到 docs
  - 验收：宠物/光影场景误报显著低于纯帧差；推理耗时与内存占用记录在案

## M4 Web 管理（里程碑：浏览器完成全部管理功能）

- [ ] T4.1 HTTP 服务与鉴权（依赖 T0.3）
  - 内容：集成 mongoose；静态资源服务；`/api/v1/auth` 登录/注销/改密（加盐哈希存储，默认账号 admin 首登强制改密）；token 鉴权中间件
  - 验收：curl 全部通过；未鉴权访问被拒
- [ ] T4.2 前端工程骨架（可与 T4.1 并行）
  - 内容：web-ui Vue3+Vite+TS 工程、登录页、主框架（侧边导航七大功能区）、构建产物进 /oem/www 的打包脚本
  - 验收：板上浏览器打开登录页并完成登录跳转；gzip 产物 < 2MB
- [ ] T4.3 实时预览（依赖 T4.1, T1.2）
  - 内容：子码流 → fMP4 over WebSocket，前端 MSE 播放；断线重连
  - 验收：Chrome 预览延迟 < 1s，连续看 10 分钟不断流
- [ ] T4.4 网络参数配置（依赖 T4.1）
  - 内容：静态 IP/DHCP、网关、DNS 查看与修改 API + 页面；改 IP 前弹确认（防失联）
  - 验收：改静态 IP 后用新 IP 能访问（此项真机验证时先征求用户同意）
- [ ] T4.5 图像与视频参数页（依赖 T4.1, T1.3）
  - 内容：亮度/对比度/饱和度、翻转镜像、码流参数（分辨率/码率/帧率/GOP）配置页
  - 验收：修改即生效或提示重启生效，预览可见变化
- [ ] T4.6 存储与回放（依赖 T4.1, T2.4）
  - 内容：TF 卡状态/格式化（高危,需二次确认）、录像检索列表、在线播放（HTTP range 拉 MP4）、下载
  - 验收：按日期检索并在网页播放/下载录像成功
- [ ] T4.7 告警与智能分析页（依赖 T4.1, T3.3）
  - 内容：侦测开关/灵敏度/区域编辑（画框）/布防时间；告警记录列表（时间+抓图）
  - 验收：页面改参数后侦测行为实际变化；告警列表与实际事件一致
- [ ] T4.8 系统管理页（依赖 T4.1）
  - 内容：设备信息、时间设置（手动+NTP 配置）、用户管理、重启、恢复出厂（高危,二次确认）、日志下载
  - 验收：各功能真机逐项验证

## M5 时间与系统完善（里程碑：整机长稳）

- [ ] T5.1 RTC 校时（依赖 T0.3）
  - 内容：开机 hwclock -s；NTP 客户端定时同步成功后 hwclock -w；时区支持
  - 验收：断电过夜（纽扣电池保持）重启后时间正确；NTP 同步日志正常
- [ ] T5.2 开机自启与守护（依赖 T0.2）
  - 内容：ipc_app 开机自启（S 脚本）、崩溃自动拉起、启动顺序处理（等待 TF 卡/网络）
  - 验收：断电重启后无人工干预全功能恢复；kill -9 后 10s 内自动拉起
- [ ] T5.3 长稳测试（依赖 M1–M4 全部）
  - 内容：连续运行 24h+：周期触发录像 + Web 预览常开；监控 RSS/存储/日志
  - 验收：无重启、无泄漏（RSS 波动 < 10%）、录像无缺段；结果记录到 worklog
- [ ] T5.4 整体代码 review 与文档终稿（依赖 T5.3）
  - 内容：对照 01/05 文档全量 review 代码；补齐 README（编译/部署/使用说明）
  - 验收：README 按步骤可从零复现部署

## M6 AOV 低功耗（二期，全部依赖 M5 完成）

- [ ] T6.1 AOV 方案调研与设计：研究 SDK/wiki 的 AOV 参考实现（suspend/快起/RTC 唤醒），产出 `docs/design/aov.md`，含功耗目标与用户决策点
- [ ] T6.2 待机-唤醒-侦测循环：RTC 定时唤醒 → 快起出流 → 单帧侦测 → 无事回眠/有事转录像（涉及内核配置，动固件前征求用户同意）
- [ ] T6.3 AOV 模式长稳与功耗实测

---

## 进度速览

| 里程碑 | 状态 |
|---|---|
| M0 环境与骨架 | 完成（T0.1–T0.3） |
| M1 视频链路 | T1.1 链路通，待光照确认验图 |
| M2 录像与存储 | 未开始 |
| M3 移动侦测 | 未开始 |
| M4 Web 管理 | 未开始 |
| M5 时间与系统完善 | 未开始 |
| M6 AOV 低功耗（二期） | 未开始 |
